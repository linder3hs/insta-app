import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#E1306C",
      contrastText: "#fff",
    },
  },
});

export default theme;
